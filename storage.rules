rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /projects/{projectId}/{allPaths=**} {
      // Anyone can read files for published/public projects or images
      allow read: if true;
      // Authenticated users can upload project assets
      allow write: if request.auth != null;
    }
  }
}
